"use client";

import { useEffect, useState } from "react";

type ExerciseTrend = { date: string; reps: number; volumeLoadKg: number; bestLoadKg: number };
type ProgressData = {
  summary: { totalSessions: number; completedSessions: number; adherence: number | null; totalVolumeLoadKg: number; totalRepetitionLoad: number; averageSessionRpe: number | null; painReports: number };
  trend: Array<{ date: string; label: string; status: string; volumeLoadKg: number; repetitionLoad: number; sessionRpe: number | null; averageRpe: number | null; averageRir: number | null }>;
  exercises: Array<{ exerciseName: string; pattern: string; sessions: number; totalReps: number; volumeLoadKg: number; bestLoadKg: number; latestLoadKg: number; latestDate: string | null; trend: ExerciseTrend[] }>;
  results: Array<{ id: string; scheduledDate: string; sessionName: string | null; status: string; sessionRpe: number | null; energy: number | null; painReported: boolean; durationMinutes: number | null; volumeLoadKg: number; repetitionLoad: number; averageRpe: number | null; averageRir: number | null; notes: string | null }>;
};

function formatKg(value: number) { return `${Math.round(value).toLocaleString("en-GB")} kg`; }
function chartPath(values: number[], width = 560, height = 170) {
  if (!values.length) return "";
  const max = Math.max(...values, 1); const min = Math.min(...values, 0); const range = max - min || 1;
  return values.map((value, index) => `${index === 0 ? "M" : "L"} ${(index / Math.max(values.length - 1, 1)) * width} ${height - ((value - min) / range) * (height - 18)}`).join(" ");
}

export function ClientProgressLauncher({ clientId }: { clientId: string }) {
  const [open, setOpen] = useState(false);
  return <>{open && <div className="modal-backdrop"><section className="progress-modal"><ClientProgressContent clientId={clientId} onClose={() => setOpen(false)} /></section></div>}<button className="client-progress-floating" onClick={() => setOpen(true)}>Client progress</button></>;
}

export function ClientProgressPanel({ clientId }: { clientId: string }) {
  return <section className="progress-panel-embedded"><ClientProgressContent clientId={clientId} /></section>;
}

function ClientProgressContent({ clientId, onClose }: { clientId: string; onClose?: () => void }) {
  const [fromDate, setFromDate] = useState(""); const [toDate, setToDate] = useState(""); const [exerciseName, setExerciseName] = useState("");
  const [data, setData] = useState<ProgressData | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { const params = new URLSearchParams({ clientId }); if (fromDate) params.set("from", fromDate); if (toDate) params.set("to", toDate); setLoading(true); fetch(`/api/designer/progress?${params}`, { credentials: "same-origin", cache: "no-store" }).then((response) => response.ok ? response.json() as Promise<ProgressData> : Promise.reject(new Error("Progress data unavailable"))).then((next) => { setData(next); setExerciseName((current) => next.exercises.some((exercise) => exercise.exerciseName === current) ? current : next.exercises[0]?.exerciseName ?? ""); }).catch((loadError) => setError(loadError instanceof Error ? loadError.message : "Progress data unavailable")).finally(() => setLoading(false)); }, [clientId, fromDate, toDate]);
  const selectedExercise = data?.exercises.find((exercise) => exercise.exerciseName === exerciseName) ?? data?.exercises[0];
  const volumeValues = data?.trend.map((item) => item.volumeLoadKg) ?? [];
  const exerciseValues = selectedExercise?.trend.map((item) => item.volumeLoadKg) ?? [];
  return <><header className="progress-panel-header"><div><p className="eyebrow">CLIENT PROGRESS</p><h2>Progress, load and performance</h2><p>Review trends across logged sessions. These metrics support PT judgement; they do not measure programme quality on their own.</p></div>{onClose && <button className="close-button" onClick={onClose}>×</button>}</header>{loading ? <div className="prompt-builder-loading">Loading progress data…</div> : error ? <p className="form-error" role="alert">{error}</p> : data ? <div className="progress-content"><div className="progress-filter-bar"><label>FROM<input type="date" value={fromDate} onChange={(event) => setFromDate(event.target.value)} /></label><label>TO<input type="date" value={toDate} onChange={(event) => setToDate(event.target.value)} /></label><button className="secondary-button" onClick={() => { setFromDate(""); setToDate(""); }}>Clear dates</button></div><div className="progress-metric-grid"><div><span>Adherence</span><strong>{data.summary.adherence === null ? "—" : `${data.summary.adherence}%`}</strong><small>{data.summary.completedSessions} completed of {data.summary.totalSessions} logged</small></div><div><span>Volume load</span><strong>{formatKg(data.summary.totalVolumeLoadKg)}</strong><small>Sum of reps × load</small></div><div><span>Repetition load</span><strong>{data.summary.totalRepetitionLoad.toLocaleString("en-GB")}</strong><small>Total logged repetitions</small></div><div><span>Average session RPE</span><strong>{data.summary.averageSessionRpe ?? "—"}</strong><small>Subjective whole-session effort</small></div></div><section className="progress-chart-card"><div className="progress-section-heading"><div><p className="eyebrow">LOAD TREND</p><h3>Volume load by session</h3></div><small>Only logged set data contributes</small></div>{volumeValues.length ? <svg className="progress-chart" viewBox="0 0 560 190" role="img" aria-label="Volume load trend"><path d={chartPath(volumeValues)} fill="none" stroke="var(--designer-teal)" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" /><line x1="0" y1="170" x2="560" y2="170" stroke="var(--designer-line)" />{volumeValues.map((value, index) => <circle key={`${value}-${index}`} cx={(index / Math.max(volumeValues.length - 1, 1)) * 560} cy={170 - (value / Math.max(...volumeValues, 1)) * 152} r="5" fill="var(--designer-teal)" />)}</svg> : <p className="progress-empty">No logged set data in this date range.</p>}</section><section className="progress-chart-card"><div className="progress-section-heading"><div><p className="eyebrow">EXERCISE FOCUS</p><h3>Exercise-specific volume</h3></div><select value={selectedExercise?.exerciseName ?? ""} onChange={(event) => setExerciseName(event.target.value)}><option value="">Select exercise</option>{data.exercises.map((exercise) => <option key={exercise.exerciseName} value={exercise.exerciseName}>{exercise.exerciseName}</option>)}</select></div>{selectedExercise && exerciseValues.length ? <svg className="progress-chart" viewBox="0 0 560 190" role="img" aria-label={`${selectedExercise.exerciseName} volume trend`}><path d={chartPath(exerciseValues)} fill="none" stroke="#7c9f75" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" /><line x1="0" y1="170" x2="560" y2="170" stroke="var(--designer-line)" />{exerciseValues.map((value, index) => <circle key={`${value}-${index}`} cx={(index / Math.max(exerciseValues.length - 1, 1)) * 560} cy={170 - (value / Math.max(...exerciseValues, 1)) * 152} r="5" fill="#7c9f75" />)}</svg> : <p className="progress-empty">No exercise results in this date range.</p>}</section><div className="progress-two-column"><section className="progress-table-card"><div className="progress-section-heading"><div><p className="eyebrow">EXERCISE DETAIL</p><h3>Exercise trends</h3></div></div>{data.exercises.length ? <div className="progress-table-wrap"><table><thead><tr><th>Exercise</th><th>Sessions</th><th>Reps</th><th>Volume</th><th>Best load</th></tr></thead><tbody>{data.exercises.map((exercise) => <tr key={exercise.exerciseName}><th>{exercise.exerciseName}<small>{exercise.pattern}</small></th><td>{exercise.sessions}</td><td>{exercise.totalReps}</td><td>{formatKg(exercise.volumeLoadKg)}</td><td>{exercise.bestLoadKg ? formatKg(exercise.bestLoadKg) : "—"}</td></tr>)}</tbody></table></div> : <p className="progress-empty">No exercise results recorded yet.</p>}</section><section className="progress-table-card"><div className="progress-section-heading"><div><p className="eyebrow">SESSION LOG</p><h3>Recent results</h3></div></div>{data.results.length ? <div className="progress-table-wrap"><table><thead><tr><th>Date</th><th>Session</th><th>Status</th><th>RPE</th><th>Volume</th></tr></thead><tbody>{data.results.slice(0, 8).map((result) => <tr key={result.id}><td>{new Date(`${result.scheduledDate}T12:00:00`).toLocaleDateString("en-GB")}</td><td>{result.sessionName ?? "Workout"}</td><td>{result.status}</td><td>{result.sessionRpe ?? "—"}</td><td>{formatKg(result.volumeLoadKg)}</td></tr>)}</tbody></table></div> : <p className="progress-empty">No workout results recorded yet.</p>}</section></div><p className="progress-footnote">Volume load is actual repetitions × actual load in kilograms. Repetition load is total actual repetitions. RPE/RIR averages should be interpreted alongside technique, pain, recovery and context.</p></div> : null}{onClose && <footer><button className="primary-button" onClick={onClose}>Close progress</button></footer>}</>;
}
