import { and, asc, desc, eq } from "drizzle-orm";
import { NextRequest, NextResponse } from "next/server";

import { getAccountAccess } from "@/lib/authorization/server";
import { getDb } from "@/lib/db/client";
import { ptAssessments, ptClients, ptExercisePrescriptions, ptExercises, ptGoals, ptLocations, ptPreferences, ptProgrammeEvents, ptProgrammeWeeks, ptProgrammes, ptSessions } from "@/lib/db/schema";

export const dynamic = "force-dynamic";

export async function GET(request: NextRequest) {
  const access = await getAccountAccess();
  if (access.state !== "active" || access.account.role !== "owner") return NextResponse.json({ error: "PT owner access required" }, { status: 403 });
  const clientId = request.nextUrl.searchParams.get("clientId");
  if (!clientId) return NextResponse.json({ error: "clientId is required" }, { status: 400 });
  const db = getDb();
  const requestedWeek = Number(request.nextUrl.searchParams.get("weekNumber"));
  const [client] = await db.select({ id: ptClients.id, firstName: ptClients.firstName, lastName: ptClients.lastName, dateOfBirth: ptClients.dateOfBirth, sexOrGender: ptClients.sexOrGender, heightCm: ptClients.heightCm, weightKg: ptClients.weightKg, occupation: ptClients.occupation, dailyActivity: ptClients.dailyActivity, sessionDurationMinutes: ptClients.sessionDurationMinutes, preferredDays: ptClients.preferredDays, sleepHours: ptClients.sleepHours, stressLevel: ptClients.stressLevel, notes: ptClients.notes }).from(ptClients).where(and(eq(ptClients.id, clientId), eq(ptClients.ownerProfileId, access.account.authUserId))).limit(1);
  if (!client) return NextResponse.json({ error: "Client not found" }, { status: 404 });
  const [assessmentRow] = await db.select({ clearanceRequired: ptAssessments.clearanceRequired, riskFlags: ptAssessments.riskFlags, responses: ptAssessments.responses, reviewDate: ptAssessments.reviewDate, assessmentDate: ptAssessments.assessmentDate, ptNotes: ptAssessments.ptNotes }).from(ptAssessments).where(eq(ptAssessments.clientId, client.id)).orderBy(desc(ptAssessments.assessmentDate)).limit(1);
  const assessment = assessmentRow ? { ...assessmentRow, injuryNotes: assessmentRow.responses && typeof assessmentRow.responses === "object" && !Array.isArray(assessmentRow.responses) ? String((assessmentRow.responses as Record<string, unknown>).injuryNotes ?? "") || null : null, contraindicationNotes: assessmentRow.responses && typeof assessmentRow.responses === "object" && !Array.isArray(assessmentRow.responses) ? String((assessmentRow.responses as Record<string, unknown>).contraindicationNotes ?? "") || null : null } : null;
  const [goal] = await db.select({ goalType: ptGoals.goalType, target: ptGoals.target, metric: ptGoals.metric }).from(ptGoals).where(and(eq(ptGoals.clientId, client.id), eq(ptGoals.priority, "primary"))).orderBy(desc(ptGoals.updatedAt)).limit(1);
  const [location] = await db.select({ name: ptLocations.name, locationType: ptLocations.locationType, equipment: ptLocations.equipment }).from(ptLocations).where(eq(ptLocations.clientId, client.id)).orderBy(desc(ptLocations.updatedAt)).limit(1);
  const [preferences] = await db.select({ likedExercises: ptPreferences.likedExercises, dislikedExercises: ptPreferences.dislikedExercises, preferredStyle: ptPreferences.preferredStyle, preferredStructure: ptPreferences.preferredStructure, preferredEquipment: ptPreferences.preferredEquipment, cardioModalities: ptPreferences.cardioModalities, varietyPreference: ptPreferences.varietyPreference, confidenceNotes: ptPreferences.confidenceNotes }).from(ptPreferences).where(eq(ptPreferences.clientId, client.id)).limit(1);
  const programmeHistory = await db.select({ id: ptProgrammes.id, name: ptProgrammes.name, goalSummary: ptProgrammes.goalSummary, status: ptProgrammes.status, version: ptProgrammes.version, updatedAt: ptProgrammes.updatedAt }).from(ptProgrammes).where(and(eq(ptProgrammes.clientId, client.id), eq(ptProgrammes.ownerProfileId, access.account.authUserId))).orderBy(desc(ptProgrammes.version));
  const [programme] = await db.select({ id: ptProgrammes.id, name: ptProgrammes.name, goalSummary: ptProgrammes.goalSummary, status: ptProgrammes.status, currentWeek: ptProgrammes.currentWeek, durationWeeks: ptProgrammes.durationWeeks, version: ptProgrammes.version, rationale: ptProgrammes.rationale }).from(ptProgrammes).where(and(eq(ptProgrammes.clientId, client.id), eq(ptProgrammes.ownerProfileId, access.account.authUserId))).orderBy(desc(ptProgrammes.updatedAt)).limit(1);
  if (!programme) return NextResponse.json({ client, assessment: assessment ?? null, goal: goal ?? null, location: location ?? null, preferences: preferences ?? null, programmeHistory, programme: null });
  const events = await db.select({ id: ptProgrammeEvents.id, action: ptProgrammeEvents.action, details: ptProgrammeEvents.details, createdAt: ptProgrammeEvents.createdAt }).from(ptProgrammeEvents).where(eq(ptProgrammeEvents.programmeId, programme.id)).orderBy(desc(ptProgrammeEvents.createdAt));
  const selectedWeekNumber = Number.isInteger(requestedWeek) && requestedWeek >= 1 && requestedWeek <= programme.durationWeeks ? requestedWeek : programme.currentWeek;
  const [week] = await db.select({ id: ptProgrammeWeeks.id, weekNumber: ptProgrammeWeeks.weekNumber, focus: ptProgrammeWeeks.focus, volumeTarget: ptProgrammeWeeks.volumeTarget, intensityTarget: ptProgrammeWeeks.intensityTarget }).from(ptProgrammeWeeks).where(and(eq(ptProgrammeWeeks.programmeId, programme.id), eq(ptProgrammeWeeks.weekNumber, selectedWeekNumber))).limit(1);
  const weekOptions = await db.select({ weekNumber: ptProgrammeWeeks.weekNumber, focus: ptProgrammeWeeks.focus, volumeTarget: ptProgrammeWeeks.volumeTarget, intensityTarget: ptProgrammeWeeks.intensityTarget }).from(ptProgrammeWeeks).where(eq(ptProgrammeWeeks.programmeId, programme.id)).orderBy(ptProgrammeWeeks.weekNumber);
  const sessions = week ? await db.select({ id: ptSessions.id, dayOfWeek: ptSessions.dayOfWeek, name: ptSessions.name, sessionType: ptSessions.sessionType, durationMinutes: ptSessions.durationMinutes, exerciseId: ptExercises.id, exerciseName: ptExercises.name, pattern: ptExercises.movementPattern, target: ptExercises.primaryMuscles, equipment: ptExercises.equipment, prescriptionId: ptExercisePrescriptions.id, orderIndex: ptExercisePrescriptions.orderIndex, sets: ptExercisePrescriptions.sets, repsMin: ptExercisePrescriptions.repsMin, repsMax: ptExercisePrescriptions.repsMax, intensityValue: ptExercisePrescriptions.intensityValue, restSeconds: ptExercisePrescriptions.restSeconds, tempo: ptExercisePrescriptions.tempo, progressionRule: ptExercisePrescriptions.progressionRule, technique: ptExercisePrescriptions.technique, notes: ptExercisePrescriptions.notes }).from(ptSessions).leftJoin(ptExercisePrescriptions, eq(ptExercisePrescriptions.sessionId, ptSessions.id)).leftJoin(ptExercises, eq(ptExercises.id, ptExercisePrescriptions.exerciseId)).where(eq(ptSessions.programmeWeekId, week.id)).orderBy(asc(ptSessions.dayOfWeek), asc(ptExercisePrescriptions.orderIndex)) : [];
  const sessionMap = new Map<string, { id: string; dayOfWeek: number; name: string; sessionType: string; durationMinutes: number; exercises: Array<{ name: string; pattern: string; prescription: string; target: string; equipment: string; exerciseId: string; prescriptionId: string; sets: number; repsMin?: number; repsMax?: number; intensityValue?: string; restSeconds?: number; tempo?: string; progressionRule?: string; method?: string; note?: string }> }>();
  for (const row of sessions) {
    const current = sessionMap.get(row.id) ?? { id: row.id, dayOfWeek: row.dayOfWeek, name: row.name, sessionType: row.sessionType, durationMinutes: row.durationMinutes, exercises: [] };
    if (row.exerciseName && row.pattern && row.sets && row.prescriptionId && row.exerciseId) current.exercises.push({ name: row.exerciseName, pattern: row.pattern, prescription: row.repsMin ? `${row.sets} × ${row.repsMin}${row.repsMax && row.repsMax !== row.repsMin ? `–${row.repsMax}` : ""}` : `${row.sets} sets`, target: Array.isArray(row.target) ? row.target.join(" · ") : "", equipment: Array.isArray(row.equipment) ? row.equipment.join(", ") : "", exerciseId: row.exerciseId, prescriptionId: row.prescriptionId, sets: row.sets, repsMin: row.repsMin ?? undefined, repsMax: row.repsMax ?? undefined, intensityValue: row.intensityValue ?? undefined, restSeconds: row.restSeconds ?? undefined, tempo: row.tempo ?? undefined, progressionRule: row.progressionRule ?? undefined, method: row.technique ?? undefined, note: row.notes ?? undefined });
    sessionMap.set(row.id, current);
  }
  return NextResponse.json({ client, assessment: assessment ?? null, goal: goal ?? null, location: location ?? null, preferences: preferences ?? null, programmeHistory, programme: { ...programme, week: week ?? null, weekOptions, sessions: Array.from(sessionMap.values()), events } });
}
